---
name: archive
description: Stash current thinking, start fresh. Use when user says "start fresh", "archive this", "clean slate".
---

# Jeeves — Archive & Fresh Start

```bash
npx tsx ${CLAUDE_PLUGIN_ROOT}/scripts/jeeves.ts --archive $ARGUMENTS
```

If no label was given, use today's date. Tell the user what was archived and where.
