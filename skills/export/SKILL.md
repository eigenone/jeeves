---
name: export
description: Generate a shareable doc for the team — all decisions, research, open questions in one file. Use when user says "give me everything for my team", "export".
---

# Jeeves — Export

```bash
npx tsx ${CLAUDE_PLUGIN_ROOT}/scripts/jeeves.ts --export
```

Tell the user the export is ready and where to find it (`thinking/export-YYYY-MM-DD.md`).
